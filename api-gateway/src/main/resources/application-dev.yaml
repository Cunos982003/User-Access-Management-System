spring:
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

  cloud:
    gateway:
      httpclient:
        pool:
          type: tcp
          max-idle-connections: 10
          min-idle-connections: 5
          acquire-timeout: 4500ms