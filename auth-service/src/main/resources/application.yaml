spring:
  application:
    name: auth-service
  config:
    activate:
      on-profile: dev

server:
  port: 8081
  servlet:
    context-path: /api/v1

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
